# Native Buzzard OS application notices and source

This directory describes the extracted native host application under `app/`.
`usr-share-doc/` is materialized from the exact dependency payload and
`project-source/` carries checksum-addressed corresponding source for Buzzard
OS. Guest/rootfs notices are kept separately under `../guest/`.

