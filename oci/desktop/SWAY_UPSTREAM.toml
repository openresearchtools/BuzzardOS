schema = 2
policy = "distribution-packages-only"
snapshot_evidence = ["oci/desktop/apt/debian-sid-snapshot.sources", "LICENSES/generated/oci-packages.tsv"]

[sway]
repository = "https://github.com/swaywm/sway"
package = "sway"
version_source = "dpkg package inventory"
license = "MIT"

[wlroots]
repository = "https://gitlab.freedesktop.org/wlroots/wlroots"
package_source = "resolved dependency of the distro sway package"
version_source = "dpkg package inventory"
license = "MIT"
