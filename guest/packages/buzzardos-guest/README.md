# buzzardos-guest

Guest mechanics package: systemd/Sway session glue, output and keyboard sync,
clipboard, media integration, AppImage support, and runtime readiness. Its
payload is defined by `guest/runtime-asset-manifest.tsv`; it does not contain
the optional Buzzard OS desktop shell or Buzzard CUA binary.
