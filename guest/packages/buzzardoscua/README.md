# buzzardoscua

Buzzard CUA package. It builds the reviewed, single-crate Linux fork under
`cua/`, installs daemonless `/usr/bin/cua` plus numbered `cuaN` multicall
links, and preserves the upstream MIT attribution alongside the AGPL terms for
Buzzard changes.
