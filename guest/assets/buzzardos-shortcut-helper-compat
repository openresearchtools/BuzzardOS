#!/bin/sh
# SPDX-License-Identifier: AGPL-3.0-or-later
set -eu

# Stable compatibility ABI for already-generated launchers and older Settings
# revisions.  The implementation is protected inside the active runtime.
exec /opt/buzzardos/runtime/current/libexec/buzzardos-shortcut-helper "$@"
